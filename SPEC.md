# LumaLog · 光屿 — 需求规格文档

> 一个自用的轻量个人博客系统。本文档是与作者讨论后敲定的全部需求，后续开发以此为准。

## 1. 项目定位

- **站点名**：LumaLog（英文）/ **光屿**（中文），意象为「光之小岛」。
- 仅作者一人使用，不做多用户、不做推广，**不追求大而全**，但**功能、效果、流畅度优先**，不为服务器配置妥协体验。
- 自建的核心动机：市面上的博客系统不够好看，且希望摆脱「改文章要操作 git」的繁琐流程。

## 2. 设计风格（核心诉求）

- **轻技术感 + 轻二次元**：有技术氛围但不硬核，有二次元气质但不出现任何人物/具象元素，只取其配色与氛围。
- 整体 UI 大气、现代、美观、有**呼吸感**（留白充足、不拥挤）。
- **视觉主题「夜航」**（2026-08-03 定稿）：普鲁士蓝的海 + 航海信号橙的光。
  - **刻意不使用多色渐变**——青紫粉三色渐变是当下 AI 产品的通用模板，是"AI 味"的主要来源。改为「中性主色 + 单一强调色」：交互与结构统一用冷色 `--accent`，"光"的时刻（置顶、灯标、图标核心）统一用暖色 `--accent-warm`。
  - **背景三层**：等高线（呼应"屿"，海图/工程图纸的克制技术感）、地平线微光（呼应"光"）、细噪点（消解纯色的数字味）。不再使用漂浮光晕球与点阵网格。
  - **图标**：等深线小岛（同心不规则轮廓 + 中心光点），与背景纹理同源；SVG 内置 `prefers-color-scheme` 以适配浅/深色标签栏。
  - 代码高亮：亮色 `github-light`、暗色 `ayu-dark`（Ayu 本身即蓝+橙配色，与主题同源且饱和度高；曾试过 `nord`，因其低饱和的设计理念导致代码发灰而弃用；`ayu-light` 的注释色对比度仅 2.0，可读性不足，故亮暗不使用同一家族）。
  - **暖光的使用原则**：暖色叠加在深色底上是"发光"，叠加在浅色底上却是"污渍"。因此亮色模式的地平线光必须用高明度的浅金（`rgba(255,205,140,.2)`，混合后为 `#f8f0e5` 的淡奶油色），若用饱和橙会压暗成土褐色。
  - **填充色上的文字**：暖色一律配深色字（`--on-warm`），这样橙色可以保持鲜亮而不必为了对比度压深；冷色在亮/暗模式分别配白字与深字（`--on-accent`）。
- 文章排版要精心设计，Markdown 渲染后**不能死板**：标题层级、引用、列表、图片、代码块都要有设计感。
- **暗色模式**：亮/暗双主题，可手动切换并跟随系统；暗色下更能体现风格，两套都要精致。

## 3. 架构与技术栈

- **形态**：带后台的动态应用（前台博客 + `/admin` 管理后台），单体应用。
- **框架**：Nuxt 3（Vue 3），SSR。
- **数据库**：SQLite（嵌入式，单文件），文章、标签、配置均存库。
- **图片**：上传后存服务器本地文件系统，由应用直接服务。
- **认证**：单管理员账密登录（凭据走环境变量/初始化配置），Session/Cookie 会话；后台所有接口需登录。
- **打包**：Docker 单容器；**镜像在本地或 GitHub Actions 构建**，服务器只拉取运行（1G 内存服务器禁止现场 build）。

## 4. 功能清单

### 前台（公开访问）

| 功能 | 说明 |
|---|---|
| 首页 | 展示最近文章列表；支持**置顶文章**优先展示；**分页浏览**（每页 6 篇，URL 带 `?page=`） |
| 文章详情页 | Markdown 渲染，排版精致；代码块带语法高亮 + 行号 + 一键复制；**右侧悬浮大纲**（正文保持居中，大纲占用右侧富余边距，滚动高亮跟随，宽屏显示/窄屏隐藏） |
| Markdown 扩展 | ①**站内标题跳转**：各级标题自动生成锚点 id，正文写 `[跳转](#标题)` 即可（大小写/标点不一致也能匹配），标题悬停显示 `#` 便于复制链接。②**提示块**：沿用 GitHub 警告块语法 `> [!TIP]`，共 5 类——NOTE 信息 / TIP 提示 / IMPORTANT 重要 / WARNING 警告 / CAUTION 错误；选它是因为在 GitHub、Obsidian 通用，不支持的渲染器上也只会退化成普通引用。③**图片尺寸**：`![图片\|600](url)`，支持 `600x400` 与 `50%`。后台编辑器有对应的快捷按钮 |
| 标签/分类 | 文章可打多个标签，可按标签筛选浏览；筛选与分页均在服务端完成，URL 形如 `/tags?t=前端&page=2`，可直接分享 |
| 站内搜索 | 按标题/内容搜索文章，**分页**展示并显示结果总数；支持服务端渲染，`/search?q=xxx` 可直接分享 |
| 归档页 | 按时间线（年/月）展示全部文章，不分页（完整时间线即为其意义），接口只返回 slug/标题/日期 |
| 暗色模式 | 手动切换 + 跟随系统 |

明确**不要**：RSS、评论、关于页、多用户。

### 后台（`/admin`，账密登录）

| 功能 | 说明 |
|---|---|
| 登录 | 单管理员，账密认证 |
| 文章管理 | 列表、新建、编辑、删除 |
| Markdown 编辑器 | 在线编辑，写作体验顺手 |
| 草稿箱 | 文章可存草稿，前台不可见 |
| 图片上传 | 编辑时直接粘贴/上传图片，自动插入文中；也可从素材库挑选已有图片，避免重复上传 |
| 素材管理 | `/admin/media`：按月分组的图片库，显示体积与总占用；**反查每张图被哪些文章引用**，未被引用的标为孤儿并可一键批量清理。删除被引用的图片会先列出相关文章要求确认。**删除文章不会自动删图**（图片可能被其他文章引用，自动删除有丢数据风险），而是让它变成可见的孤儿由人工清理。不做服务端缩略图——那需要 sharp 等原生依赖，与"零原生依赖 / 1G 机器"的取向冲突，改用原图 + 懒加载 + CSS 裁剪 |
| 置顶 | 可将文章设为置顶 |
| 标签管理 | 创建/编辑标签 |

## 5. 部署方案

- **目标服务器**：Oracle 免费云主机（现有 1G 内存 AMD 机型即可跑；建议尝试免费 ARM A1.Flex 升配，非必须）。
- **域名**：作者已有域名，解析子域名到服务器。
- **反向代理**：Caddy，自动申请/续期 HTTPS 证书。
- **运行**：Docker Compose（应用容器 + Caddy），SQLite 数据文件与图片目录挂载卷持久化。
- **关键环境变量**：`NUXT_ADMIN_USERNAME` / `NUXT_ADMIN_PASSWORD`（管理员账密，开发默认 admin / lumalog-dev，**生产必须覆盖**）、`NUXT_SESSION_PASSWORD`（会话加密密钥，32 位以上随机串）、`NUXT_DATA_DIR`（数据目录，默认 `.data`，存放 SQLite 与上传图片）。
- **更新流程**：本地/CI 构建镜像 → 服务器拉取 → 重启容器。
- 运行内存预算约 150–250MB，1G 内存 + swap 兜底，绰绰有余。

## 6. 已明确的取舍记录

- 免费服务器（Oracle/GCP 1G）经评估**足以运行后台方案**，故不采用 Vercel/GitHub Pages 静态方案。
- 作者不愿为服务器付费；开发以功能与体验为先，性能适配通过「不在服务器构建」等工程手段解决，而非砍功能。
